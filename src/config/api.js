export default {
  /**
   * The `baseURL` will be prepended to `url` unless the `url` is absolute.
   * @type {String}
   */
  baseURL: process.env.API_BASE_URL,

  /**
   * The timezone of datetime formats.
   * @type {String}
   */
  timezone: 'UTC'
};
